import { artifactFields } from '@/lib/stem/stages';
import type { STEMTask,StageId,LearningArtifacts as Records } from '@/types';
export function LearningArtifacts({task,stage,records,onChange,onRecord}:{task:STEMTask;stage:StageId;records:Records;onChange:(field:string,value:string)=>void;onRecord?:()=>void}) {
 return <details className="rail-section learning-artifacts" open><summary>Your Thinking</summary><p>Capture your ideas for this stage. Saved in this browser tab.</p>{artifactFields(task,stage).slice(0,3).map(field=><label key={field}>{field}<textarea maxLength={400} rows={2} value={records[stage]?.[field]??''} onBlur={onRecord} onChange={e=>onChange(field,e.target.value)} placeholder="Your words, your evidence…"/></label>)}{artifactFields(task,stage).length>3&&<details><summary>More thinking prompts</summary>{artifactFields(task,stage).slice(3).map(field=><label key={field}>{field}<textarea maxLength={400} rows={2} value={records[stage]?.[field]??''} onBlur={onRecord} onChange={e=>onChange(field,e.target.value)}/></label>)}</details>}</details>;
}
